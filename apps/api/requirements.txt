# Core
# 5.2.17 is the first patch release without known CVEs (PYSEC-2026-3717 et al).
django>=5.2.17,<5.3
django-environ>=0.11

# REST API
djangorestframework>=3.16
djangorestframework-simplejwt>=5.5
django-cors-headers>=4.7
# OpenAPI 3 schema + Swagger UI at /api/docs/, generated from the views.
drf-spectacular>=0.30

# WebSocket
channels>=4.2
channels-redis>=4.2
daphne>=4.2

# Static files (prod: Daphne does not serve static; WhiteNoise does)
whitenoise>=6.6

# Database
psycopg2-binary>=2.9
# psycopg[c]>=3.2  # Uncomment for psycopg 3 support

# Geospatial
# GDAL may need system-level install: https://docs.djangoproject.com/en/5.2/ref/contrib/gis/install/

# Utilities
# 12.x is the first line without the 2026 Pillow CVE batch (PYSEC-2026-2249+).
Pillow>=12.3
argon2-cffi>=23.1
# Web-push VAPID (notifications/services.py) and dev certs depend on this;
# pin it explicitly instead of riding a transitive install.
cryptography>=50
# Security floors for transitive dependencies flagged by pip-audit.
sqlparse>=0.6
python-multipart>=0.0.31

# Async
httpx>=0.28
requests>=2.31,<3
firebase-admin>=7,<8
celery[redis]>=5.4,<6

# Forensics / Media authenticity
c2pa-python>=0.5.0
imagehash>=4.3,<5
opencv-python-headless>=4.8,<5

# Concern review (Gemma) and privacy segmentation (SAM3 via Roboflow)
# SAM3 is called over plain HTTP with `requests` above rather than through
# roboflow's `inference-sdk`: every published version of that package requires
# Python <3.13, and this project runs 3.13.
ollama>=0.5,<1

# Street-level imagery search (Google panorama lookup near a concern pin).
# Tile downloads are handled by apps.concerns.ai.street_imagery itself.
streetview>=0.1

# Official OCR template testing
easyocr>=1.7,<2
cloudinary==1.45.0
django-cloudinary-storage==0.3.0
