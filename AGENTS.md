## Git restriction

Do not run any Git commands or modify anything inside `.git`. This includes read-only commands such as `git status`, `git diff`, and `git log`. Do not commit, switch branches, reset, restore, merge, rebase, pull, push, or create branches or tags.

If Git access is necessary, stop and ask the user for explicit permission first.
