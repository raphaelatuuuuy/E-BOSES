"""
Notifications views for E-Boses.

Planned endpoints:
    - GET /api/notifications/           — List user notifications
    - PATCH /api/notifications/:id/read/ — Mark as read
    - POST /api/notifications/read-all/ — Mark all as read
"""
