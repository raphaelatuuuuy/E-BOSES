"""
Concerns views for E-Boses.

Planned endpoints:
    - GET/POST /api/concerns/           — List / Create concerns
    - GET/PUT/PATCH /api/concerns/:id/ — Retrieve / Update concern
    - DELETE /api/concerns/:id/        — Delete concern
    - GET /api/concerns/:id/media/     — Concern media attachments
    - GET /api/concerns/categories/    — Concern categories
    - GET /api/concerns/nearby/        — Geospatial nearby query
"""
