"""
Concerns models for E-Boses.

Planned models:
    - Concern (title, description, geolocation, category, status)
    - ConcernMedia (photo/video evidence attached to a concern)
    - ConcernStatusLog (audit trail of status changes)
    - Category (concern categories like road, waste, utilities, etc.)
"""

# from django.contrib.gis.db import models
