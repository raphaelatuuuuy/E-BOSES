// Stub API functions for frontend development
// Replace with full API client implementation later

export const api = {
  get: (_endpoint: string) => Promise.resolve({}),
  post: (_endpoint: string, _body?: unknown) => Promise.resolve({}),
  patch: (_endpoint: string, _body?: unknown) => Promise.resolve({}),
  delete: (_endpoint: string) => Promise.resolve({}),
};

export const setTokens = () => {};
export const clearTokens = () => {};
export const hasTokens = () => false;